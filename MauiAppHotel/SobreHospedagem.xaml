<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppHotel.Views.SobreHospedagem"
             Title="Sobre"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="{StaticResource Gray100}">
             
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">
            <Label
                Text="SOBRE O PROJETO"
                FontFamily="Kalam"
                FontSize="30"
                HorizontalOptions="Center"            
                TextColor="{StaticResource Primary}" />
            <Frame
                BorderColor="Black"
                Padding="10"
                CornerRadius="5">
            <Label
                Text="Desenvolvedor: Gláucio M. Pereira
                Ano de Desenvolvimento: 2025
                TURMA EHO - TO250015239D2"
                FontSize="Micro"
                HorizontalOptions="Center" />
            </Frame>
            
            <Frame
                BorderColor="Black"
                Padding="10"
                CornerRadius="5">
                <Label
                    Text="Atendendo a uma demanda atual, este aplicativo cria check-in para quartos em hoteis."
                    FontSize="Micro"
                    HorizontalTextAlignment="Center"
                    HorizontalOptions="Start" />
            </Frame>

            <Button
                Text="Voltar para Reserva"
                Clicked="OnVoltarButtonClicked"
                BackgroundColor="{StaticResource Black}"
                FontAttributes="Bold"
                WidthRequest="220"
                HorizontalOptions="Center" />
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>